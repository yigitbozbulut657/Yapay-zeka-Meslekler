# Global Rules

- **Git Push Operations:** NEVER execute `git push` or similar remote repository upload commands automatically. Always propose the command and wait for explicit user approval before executing it.
